<!--
	Purpose: Reserve the internal components area for the app-launcher block family.
	Context: The family is currently implemented in a single root component, but the ui-system schema still expects this folder.
	Responsibility: Document that future internal subcomponents should stay private to the family.
	Boundaries: Nothing in this folder should be imported directly outside the family.
-->

# `app-launcher/components`

Internal view pieces for the `app-launcher` block family belong here if the family grows beyond its current single-file implementation.
