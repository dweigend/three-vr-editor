/**
 * Purpose: Hold shared family metadata for the toolbar primitive family.
 * Context: The local ui-system-aligned component layer keeps lightweight metadata per family.
 * Responsibility: Provide a stable family identifier for documentation and future tooling.
 * Boundaries: This module does not render UI or own toolbar state.
 */

export const toolbarFamily = 'toolbar';
