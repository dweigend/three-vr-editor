<!--
	Purpose: Document the internal scroll-area family components.
	Context: The family currently exposes a single styled root wrapper around Bits UI scroll primitives.
	Responsibility: Keep implementation details private to the family and preserve public imports through `index.ts`.
	Boundaries: External consumers should not deep-import from this folder.
-->

# `scroll-area/components`

Internal view pieces for the `scroll-area` primitive family.
