/**
 * Shared family metadata for the icon-button primitive.
 * Keep cross-file constants here if the family grows beyond its current single component.
 */
export const iconButtonFamily = 'icon-button';
