<!--
	Purpose: Describe the compact icon-first button primitive used by the global shell and utility actions.
	Context: The square app chrome needs a reusable icon trigger that stays aligned with the local ui-system patterns.
	Responsibility: Define the public family boundary while keeping the implementation thin and presentational.
	Boundaries: The family must not absorb menu state, routing logic, or app-specific behavior.
-->

# `icon-button`

Primitive UI family for compact icon-first actions.
