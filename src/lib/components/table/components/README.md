<!--
	Purpose: Document the internal subcomponents of the table primitive family.
	Context: The table family is composed of semantic wrappers around native table elements.
	Responsibility: Keep the part components private to the family and preserve a consistent public API through `index.ts`.
	Boundaries: External consumers should import from the family barrel only, never from this folder directly.
-->

# `table/components`

Internal semantic wrappers for the `table` primitive family.
