/**
 * Purpose: Hold shared family metadata for the command-input primitive.
 * Context: The local ui-system-aligned component layer keeps lightweight metadata per family.
 * Responsibility: Provide a stable family identifier for docs and future tooling.
 * Boundaries: This module does not render UI or own runtime behavior.
 */

export const commandInputFamily = 'command-input';
