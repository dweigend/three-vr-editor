/**
 * Purpose: Hold shared family metadata for the textarea primitive.
 * Context: The local ui-system-aligned component layer keeps a metadata file per family.
 * Responsibility: Provide a stable family identifier for future tooling or docs.
 * Boundaries: This module does not render UI or own runtime behavior.
 */

export const textareaFamily = 'textarea';
